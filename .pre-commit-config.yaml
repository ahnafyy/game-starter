repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.8
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(agents|tests|tools)/
      - id: ruff-format
        files: ^(agents|tests|tools)/
