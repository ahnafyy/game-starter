role: level-designer
model: claude-opus-4-5
allowed_tools:
  - create_object
  - modify_object
  - get_scene_info
  - execute_python
description: >
  Places actors, lighting, blocking volumes, and encounter triggers in UE maps
  via MCP. Responsible for spatial layout and playable area setup.
escalation:
  on_navmesh_failure: flag to human
  on_out_of_bounds_placement: auto-correct to level bounds, then flag
